## Running java and postgres on Docker-Compose


## Build a jar file for the application with the command,

```sh
mvn clean install -DskipTests
```

Then run `docker-compose up` to start the application.
